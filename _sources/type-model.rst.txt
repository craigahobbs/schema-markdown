.. _schema:

Type Model
==========

At the heart of Schema Markdown is the **type model**. The type model is a dictionary object that
maps schema type names to schema type definitions. The :ref:`parser` definition of type model is
shown below:

.. include:: ../build/doc/type_model.smd
   :code: text
